import { Collection } from './Collection'

/**
* A sequence is an ordered collection of elements in wich repetitions are
* allowed. A sequence may be infinite.
*/
export interface Sequence<Element> extends Collection<Element> {

}
